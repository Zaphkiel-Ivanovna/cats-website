---
import Default from '@astrojs/starlight/components/Hero.astro';
import Landing from './landing/Landing.astro';

const isLanding = Astro.locals.starlightRoute.entry.data.template === 'splash';
---

{isLanding ? <Landing /> : <Default />}
